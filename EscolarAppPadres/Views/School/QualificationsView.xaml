<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:syncfusion="clr-namespace:Syncfusion.Maui.DataGrid;assembly=Syncfusion.Maui.DataGrid"
             xmlns:local="clr-namespace:EscolarAppPadres.Helpers"
             xmlns:viewModels="clr-namespace:EscolarAppPadres.ViewModels.StudentGrade"
             x:Class="EscolarAppPadres.Views.School.QualificationsView"
             Title="ESCOLAR"
             Padding="{OnPlatform iOS='0,20,0,0', Android='0'}"
             BackgroundColor="#f5f8fa">

    <ContentPage.Resources>
        <ResourceDictionary>
            <local:CalificacionColorConverter x:Key="CalificacionColorConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Mensaje si no hay resultados -->
        <Label Grid.Row="0"
               Text="No se encontraron calificaciones"
               FontSize="16"
               TextColor="Gray"
               HorizontalOptions="Center"
               Margin="15"
               IsVisible="{Binding SinResultados}" />

        <!-- DataGrid con RefreshView -->
        <RefreshView Grid.Row="1"
                     Command="{Binding LoadGradesCommand}"
                     IsRefreshing="{Binding IsRefreshing}"
                     Margin="15,10">

            <syncfusion:SfDataGrid x:Name="dataGrid"
                                   ItemsSource="{Binding MateriasConCalificaciones}"
                                   AutoGenerateColumnsMode="None"
                                   HeaderRowHeight="50"
                                   RowHeight="45"
                                   GridLinesVisibility="Both"
                                   HeaderGridLinesVisibility="Both"
                                   SelectionMode="Single"
                                   VerticalOptions="FillAndExpand"
                                   HorizontalOptions="FillAndExpand">

                <!-- Columna fija para materia -->
                <syncfusion:SfDataGrid.Columns>
                    <syncfusion:DataGridTextColumn HeaderText="Materia"
                                                   MappingName="MateriaNombre"
                                                   Width="150"
                                                   HeaderTextAlignment="Center" />
                </syncfusion:SfDataGrid.Columns>
            </syncfusion:SfDataGrid>
        </RefreshView>

        <!-- Picker flotante para seleccionar hijo -->
        <Frame Grid.Row="2"
               BackgroundColor="White"
               CornerRadius="20"
               HasShadow="True"
               Padding="6,2"
               Margin="0,0,0,25"
               HeightRequest="44"
               WidthRequest="280"
               HorizontalOptions="Center"
               VerticalOptions="End">
            <Picker Title="Seleccionar hijo"
                    ItemsSource="{Binding Hijos}"
                    ItemDisplayBinding="{Binding NombreCompleto}"
                    SelectedItem="{Binding SelectedHijo}"
                    FontSize="13"
                    TextColor="#273840"
                    HorizontalOptions="FillAndExpand" />
        </Frame>
    </Grid>
</ContentPage>