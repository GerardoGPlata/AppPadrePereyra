<?xml version="1.0" encoding="utf-8" ?>
<syncfusionPopup:SfPopup
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:syncfusionPopup="clr-namespace:Syncfusion.Maui.Popup;assembly=Syncfusion.Maui.Popup"
    xmlns:helpers="clr-namespace:EscolarAppPadres.Helpers"
    x:Class="EscolarAppPadres.Views.Account.ColorPickerPopup"
    x:Name="Root"
    StaysOpen="True"
    OverlayMode="Blur"
    ShowCloseButton="True"
    AutoSizeMode="Height"
    AnimationMode="Fade"
    HeaderTitle="Selecciona un color">

    <syncfusionPopup:SfPopup.PopupStyle>
        <syncfusionPopup:PopupStyle HeaderBackground="#273840"
                                    HeaderFontSize="18"
                                    HeaderTextColor="White"
                                    PopupBackground="White"
                                    CloseButtonIcon="close_icon"
                                    BlurIntensity="Dark"/>
    </syncfusionPopup:SfPopup.PopupStyle>

    <syncfusionPopup:SfPopup.ContentTemplate>
        <DataTemplate>
            <Grid Padding="16"
                  RowSpacing="0">
                <Grid.Resources>
                    <helpers:ColorConverter x:Key="ColorConverter"/>
                    <helpers:NotNullToBoolConverter x:Key="NotNullToBoolConverter"/>
                    <helpers:SelectedColorBorderMultiConverter x:Key="SelectedColorBorderMultiConverter"/>
                    <helpers:SelectedColorToOpacityMultiConverter x:Key="SelectedColorToOpacityMultiConverter"/>
                </Grid.Resources>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Preview seleccionado -->
                <Grid Grid.Row="0"
                      ColumnDefinitions="Auto,*"
                      ColumnSpacing="10"
                      Padding="0,4,0,12">
                    <Grid WidthRequest="34"
                          HeightRequest="34"
                          VerticalOptions="Center">
                        <Ellipse>
                            <Ellipse.Fill>
                                <SolidColorBrush Color="{Binding Source={x:Reference Root}, Path=SelectedColor, Converter={StaticResource ColorConverter}}"/>
                            </Ellipse.Fill>
                        </Ellipse>
                    </Grid>
                    <Label Grid.Column="1"
                           Text="{Binding Source={x:Reference Root}, Path=SelectedColor}"
                           FontSize="14"
                           TextColor="#273840"
                           VerticalOptions="Center"/>
                </Grid>

                <!-- Paleta de colores -->
                <CollectionView
                    Grid.Row="1"
                    ItemsSource="{Binding Source={x:Reference Root}, Path=Colores}"
                    SelectionMode="None">
                    <CollectionView.ItemsLayout>
                        <GridItemsLayout Orientation="Vertical"
                                         Span="5"
                                         HorizontalItemSpacing="10"
                                         VerticalItemSpacing="10"/>
                    </CollectionView.ItemsLayout>

                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Grid InputTransparent="False"
                                  BackgroundColor="Transparent">
                                <Border WidthRequest="38"
                                        HeightRequest="38"
                                        StrokeThickness="0"
                                        BackgroundColor="Transparent"
                                        HorizontalOptions="Center"
                                        VerticalOptions="Center">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="19"/>
                                    </Border.StrokeShape>

                                    <Grid>
                                        <!-- Círculo de color sin stroke -->
                                        <Ellipse InputTransparent="True">
                                            <Ellipse.Fill>
                                                <SolidColorBrush Color="{Binding ., Converter={StaticResource ColorConverter}}"/>
                                            </Ellipse.Fill>
                                        </Ellipse>

                                        <!-- Indicador de selección -->
                                        <Image Source="checked_icon"
                                               WidthRequest="20"
                                               HeightRequest="20"
                                               HorizontalOptions="Center"
                                               VerticalOptions="Center"
                                               InputTransparent="True">
                                            <Image.Opacity>
                                                <MultiBinding Converter="{StaticResource SelectedColorToOpacityMultiConverter}">
                                                    <Binding/>
                                                    <Binding Source="{x:Reference Root}"
                                                             Path="SelectedColor"/>
                                                </MultiBinding>
                                            </Image.Opacity>
                                        </Image>
                                    </Grid>
                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer
                                            Command="{Binding Source={x:Reference Root}, Path=ColorTappedCommand}"
                                            CommandParameter="{Binding .}"/>
                                    </Border.GestureRecognizers>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <!-- Sin botones: autoaceptar en tap -->
            </Grid>
        </DataTemplate>
    </syncfusionPopup:SfPopup.ContentTemplate>
</syncfusionPopup:SfPopup>
